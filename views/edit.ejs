<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD Operation</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-gray-100 p-5 ">
        <div class="nav flex gap-3">
            <h1 class="text-4xl mb-5">Edit User</h1>
            <a href="/read" class="border-2 border-zinc-400 px-3 py-2 rounded-lg mb-5 hover:cursor-pointer ">View All</a>
        </div>
        <form action="/update/<%= user._id %>" method="post">
            <input type="text" class="mr-1 px-3 py-2 bg-transparent rounded-md border-2 border-zinc-400 outline-none text-xl" placeholder="name" name="name" value="<%= user.name %>">
            <input type="text" class="mr-1 px-3 py-2 bg-transparent rounded-md border-2 border-zinc-400 outline-none text-xl" placeholder="email" name="email" value="<%= user.email %>">
            <input type="text" class="mr-1 px-3 py-2 bg-transparent rounded-md border-2 border-zinc-400 outline-none text-xl" placeholder="Image url" name="image" value="<%= user.image %>">
            <input class="mr-1 px-3 py-2 border-2 bg-green-600 text-xl rounded-md cursor-pointer hover:bg-sky-700 text-white" type="submit" value="Update user">
        </form>
    </div>
</body>
</html>